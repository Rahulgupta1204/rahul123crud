@model IEnumerable<mvccrudoperations.Models.product_master>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.product_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.product_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.product_category)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.product_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.product_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.product_category)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { item.product_id }) |
                @Html.ActionLink("Details", "Details", new { item.product_id }) |
                @Html.ActionLink("Delete", "Delete", new { item.product_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
